<% if item.favorited_by?(current_user) %>
  <%= link_to item_favorites_path(item), method: :delete, remote: true, class: 'text-ratyyellow text-xl' do %>
    <i class="fas fa-bookmark"></i>
  <% end %>
<% else %>
  <%= link_to item_favorites_path(item), method: :post, remote: true, class: 'text-gray-400 hover:text-ratyyellow text-xl' do %>
    <i class="far fa-bookmark"></i>
  <% end %>
<% end %>

