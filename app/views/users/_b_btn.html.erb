<% if item.favorited_by?(current_user) %>
  <%= link_to item_favorites_path(item), method: :delete, remote: true, class: 'text-ratyyellow xs:text-xs sm:text-base' do %>
    <i class="fas fa-bookmark"></i>
  <% end %>
<% else %>
  <%= link_to item_favorites_path(item), method: :post, remote: true, class: 'text-gray-400 hover:text-ratyyellow xs:text-xs sm:text-base' do %>
    <i class="far fa-bookmark"></i>
  <% end %>
<% end %>

