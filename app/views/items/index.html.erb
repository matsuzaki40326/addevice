<div id="cga" class="relative overflow-hidden bg-no-repeat bg-cover bg-center h-screen">
  <div class="absolute top-0 right-0 bottom-0 left-0 w-full h-full overflow-hidden bg-fixed"style="background-color: rgba(0, 0, 0, 0.75)">
    <div class="flex justify-center h-full">
      <div class="text-center text-white px-6 md:px-12">
        <h1 class="text-5xl font-black mt-52 mt-0 mb-8">addevice</h1>
        <h3 class="text-xl font-bold">device + advice</h3>
      </div>
    </div>
</div>

<div class="flex absolute xs:bottom-10 sm:bottom-20 overflow-hidden">
  <div class="slidebox flex items-center overflow-hidden slidestop" style="height: 258px;">
    <div class="slideshow">
      <% @items.each do |item| %>
        <div class="p-0.5 bg-gradient-to-tr from-purple-800 to-pink-800 rounded-lg transform hover:-translate-y-3 duration-500" id="marginclass">
          <%= link_to item_path(item) do %>
            <div class="content"><%= image_tag item.image.variant(resize: "700x700^", gravity: "center", crop: "700x700+0+0").processed, class: 'rounded-lg  shadow-md hover:opacity-75' %></div>
          <% end %>
        </div>
      <% end %>
    </div>
    <div class="slideshow">
      <% @items.each do |item| %>
        <div class="p-0.5 bg-gradient-to-tr from-purple-800 to-pink-800 rounded-lg transform hover:-translate-y-3 duration-500" id="marginclass">
          <%= link_to item_path(item) do %>
            <div class="content"><%= image_tag item.image.variant(resize: "700x700^", gravity: "center", crop: "700x700+0+0").processed, class: 'rounded-lg shadow-md hover:opacity-75' %></div>
          <% end %>
        </div>
      <% end %>
    </div>
    <div class="slideshow">
      <% @items.each do |item| %>
        <div class="p-0.5 bg-gradient-to-tr from-purple-800 to-pink-800 rounded-lg transform hover:-translate-y-3 duration-500" id="marginclass">
          <%= link_to item_path(item) do %>
            <div class="content"><%= image_tag item.image.variant(resize: "700x700^", gravity: "center", crop: "700x700+0+0").processed, class: 'rounded-lg shadow-md hover:opacity-75' %></div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>